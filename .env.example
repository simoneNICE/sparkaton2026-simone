# Future config (not used yet — routing is deterministic and needs no keys).
#
# When wiring real answers:
#  - Claude CLI path (if the "Run real answer via Claude CLI" toggle is enabled)
# CLAUDE_CLI_PATH=claude
#
#  - Or direct provider API keys
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
